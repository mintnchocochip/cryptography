import socket
from typing import List, Tuple

S_BOX = []

INV_S_BOX = []

RCON = [0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36]


def gmul(a: int, b: int) -> int:
    p = 0
    for _ in range(8):
        if b & 1:
            p ^= a
        carry = a & 0x80
        a = (a << 1) & 0xFF
        if carry:
            a ^= 0x1B
        b >>= 1
    return p & 0xFF


def add_round_key(state: List[int], round_key: List[int]) -> List[int]:
    return [s ^ k for s, k in zip(state, round_key)]


def sub_bytes(state: List[int]) -> List[int]:
    return [S_BOX[b] for b in state]


def inv_sub_bytes(state: List[int]) -> List[int]:
    return [INV_S_BOX[b] for b in state]


def shift_rows(state: List[int]) -> List[int]:
    out = state[:]
    out[1], out[5], out[9], out[13] = state[5], state[9], state[13], state[1]
    out[2], out[6], out[10], out[14] = state[10], state[14], state[2], state[6]
    out[3], out[7], out[11], out[15] = state[15], state[3], state[7], state[11]
    return out


def inv_shift_rows(state: List[int]) -> List[int]:
    out = state[:]
    out[1], out[5], out[9], out[13] = state[13], state[1], state[5], state[9]
    out[2], out[6], out[10], out[14] = state[10], state[14], state[2], state[6]
    out[3], out[7], out[11], out[15] = state[7], state[11], state[15], state[3]
    return out


def mix_single_column(col: List[int]) -> List[int]:
    return [
        gmul(col[0], 2) ^ gmul(col[1], 3) ^ col[2] ^ col[3],
        col[0] ^ gmul(col[1], 2) ^ gmul(col[2], 3) ^ col[3],
        col[0] ^ col[1] ^ gmul(col[2], 2) ^ gmul(col[3], 3),
        gmul(col[0], 3) ^ col[1] ^ col[2] ^ gmul(col[3], 2),
    ]


def inv_mix_single_column(col: List[int]) -> List[int]:
    return [
        gmul(col[0], 14) ^ gmul(col[1], 11) ^ gmul(col[2], 13) ^ gmul(col[3], 9),
        gmul(col[0], 9) ^ gmul(col[1], 14) ^ gmul(col[2], 11) ^ gmul(col[3], 13),
        gmul(col[0], 13) ^ gmul(col[1], 9) ^ gmul(col[2], 14) ^ gmul(col[3], 11),
        gmul(col[0], 11) ^ gmul(col[1], 13) ^ gmul(col[2], 9) ^ gmul(col[3], 14),
    ]


def mix_columns(state: List[int]) -> List[int]:
    out = []
    for c in range(4):
        col = state[c * 4 : c * 4 + 4]
        out.extend(mix_single_column(col))
    return out


def inv_mix_columns(state: List[int]) -> List[int]:
    out = []
    for c in range(4):
        col = state[c * 4 : c * 4 + 4]
        out.extend(inv_mix_single_column(col))
    return out


def rot_word(word: List[int]) -> List[int]:
    return word[1:] + word[:1]


def sub_word(word: List[int]) -> List[int]:
    return [S_BOX[b] for b in word]


def expand_key(key_bytes: List[int]) -> List[List[int]]:
    assert len(key_bytes) == 16, "Key must be 16 bytes for AES-128"
    words = [key_bytes[i : i + 4] for i in range(0, 16, 4)]
    for i in range(4, 44):
        temp = words[i - 1][:]
        if i % 4 == 0:
            temp = sub_word(rot_word(temp))
            temp[0] ^= RCON[i // 4]
        new_word = [a ^ b for a, b in zip(words[i - 4], temp)]
        words.append(new_word)

    round_keys = []
    for r in range(11):
        round_key = []
        for w in words[r * 4 : r * 4 + 4]:
            round_key.extend(w)
        round_keys.append(round_key)
    return round_keys


def hex_state(state: List[int]) -> str:
    return "".join(f"{b:02X}" for b in state)


def bytes_from_hex(hex_str: str) -> List[int]:
    if len(hex_str) % 2 != 0:
        raise ValueError("Hex string length must be even")
    return [int(hex_str[i : i + 2], 16) for i in range(0, len(hex_str), 2)]


def pkcs7_unpad(data: bytes) -> bytes:
    if not data:
        return data
    pad_len = data[-1]
    if pad_len == 0 or pad_len > 16:
        return data
    if data[-pad_len:] != bytes([pad_len]) * pad_len:
        return data
    return data[:-pad_len]


def decrypt_block(
    block: List[int], round_keys: List[List[int]]
) -> Tuple[List[int], List[str]]:
    state = block[:]
    logs = [f"Cipher Block: {hex_state(state)}"]

    state = add_round_key(state, round_keys[10])
    logs.append(f"Round 10 (AddRoundKey): {hex_state(state)}")

    for r in range(9, 0, -1):
        state = inv_shift_rows(state)
        state = inv_sub_bytes(state)
        state = add_round_key(state, round_keys[r])
        state = inv_mix_columns(state)
        logs.append(
            f"Round {r} (InvShiftRows+InvSubBytes+AddRoundKey+InvMixColumns): {hex_state(state)}"
        )

    state = inv_shift_rows(state)
    state = inv_sub_bytes(state)
    state = add_round_key(state, round_keys[0])
    logs.append(
        f"Round 0 (Final InvShiftRows+InvSubBytes+AddRoundKey): {hex_state(state)}"
    )

    return state, logs


def decrypt_message(cipher_hex: str, key_hex: str) -> Tuple[str, List[str]]:
    key_bytes = bytes_from_hex(key_hex)
    round_keys = expand_key(key_bytes)

    if len(cipher_hex) % 32 != 0:
        raise ValueError("Cipher text hex length must be multiple of 32 (16 bytes)")

    logs: List[str] = []
    plaintext_bytes: List[int] = []
    for i in range(0, len(cipher_hex), 32):
        block_hex = cipher_hex[i : i + 32]
        block_bytes = bytes_from_hex(block_hex)
        plain_block, block_logs = decrypt_block(block_bytes, round_keys)
        logs.extend([f"Block {i // 32} - {entry}" for entry in block_logs])
        plaintext_bytes.extend(plain_block)

    plaintext = pkcs7_unpad(bytes(plaintext_bytes)).decode(errors="ignore")
    return plaintext, logs


def receive_payload(host: str = "127.0.0.1", port: int = 65432) -> str:
    client = socket.socket()
    try:
        client.connect((host, port))
        payload = client.recv(65536).decode()
        client.close()
        return payload
    except Exception as exc:
        client.close()
        raise RuntimeError(f"Connection error: {exc}") from exc




def main() -> None:
    payload = receive_payload()

    if ":" not in payload:
        raise ValueError("Payload must be in 'key:ciphertext' format (hex strings).")

    key_hex, cipher_hex = payload.split(":", 1)

    key_hex = key_hex.strip()
    cipher_hex = cipher_hex.strip()

    print("\nReceived:")
    print(f"Key (hex, 32 chars): {key_hex}")
    print(f"Cipher Text (hex):   {cipher_hex}")

    if len(key_hex) != 32 or any(ch not in "0123456789abcdefABCDEF" for ch in key_hex):
        print("Invalid key: must be 32 hex characters (128 bits).")
        return

    try:
        plaintext, logs = decrypt_message(cipher_hex, key_hex)
    except ValueError as exc:
        print(f"Decryption failed: {exc}")
        return

    print("\nIntermediate Results (Rounds 10..0) in HEX format:")
    for entry in logs:
        print(entry)

    print("\nPlaintext after decryption:")
    print(plaintext)
    print("=" * 60)


if __name__ == "__main__":
    main()
